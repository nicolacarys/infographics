import React from "react";

export default ({ }) => (
    <div>
    		<label for="skill-data">Enter a numeric value for your skill-level</label>
        <input type="number" id="skill-data" name="skill-data" />
    </div>
);